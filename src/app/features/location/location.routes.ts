import { Routes } from '@angular/router';
import { LocationPickerComponent } from './pages/location-picker/location-picker.component';

export const LOCATION_ROUTES: Routes = [
  {
    path: '',
    component: LocationPickerComponent
  }
];
