import { Routes } from '@angular/router';
import { ClosedStoreComponent } from './components/closed-store/closed-store';

export const SERVICE_STATUS_ROUTES: Routes = [
  {
    path: '',
    component: ClosedStoreComponent
  }
];
